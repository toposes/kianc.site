source "https://rubygems.org"

# Plain Jekyll. This site uses no GitHub Pages-only plugins and the default
# kramdown markdown engine, so local output matches the deployed site.
gem "jekyll", "~> 4.3"

# Ruby no longer bundles webrick, which `jekyll serve` needs.
gem "webrick"

destination: _build
title: Kian Cho
description: Hello, I am Kian (he/him). I am a category theory enjoyer, a stack of cats in trench coat, or a horrible goose.
logo: assets/portrait.png
baseurl: ""
collections:
  pages:
    output: true

# Apply the default layout everywhere so pages don't each have to declare it.
defaults:
  - scope:
      path: ""
    values:
      layout: default


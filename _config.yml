destination: _build
title: Kian Cho
baseurl: ""
collections:
  pages:
    output: true

# Apply the default layout everywhere so pages don't each have to declare it.
defaults:
  - scope:
      path: ""
    values:
      layout: default


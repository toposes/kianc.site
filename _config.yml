destination: _build
title: Kian Cho
baseurl: ""

# Repo files that shouldn't end up on the live site.
exclude:
  - README.md

collections:
  pages:
    output: true

# Apply the default layout everywhere so pages don't each have to declare it.
defaults:
  - scope:
      path: ""
    values:
      layout: default

